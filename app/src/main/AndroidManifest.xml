<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clash.mv1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".option" />
        <activity android:name=".manual" />
        <activity android:name=".lineTracing" />
        <activity android:name=".homeAuto" />
        <activity android:name=".gyro" />
        <activity android:name=".speechControl" />
        <activity android:name=".start" />
        <activity android:name=".info"></activity>
    </application>

</manifest>